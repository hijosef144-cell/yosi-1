# 🚀 הוראות העלאה ל-Netlify - מצפן כלכלי

## 📋 רשימת קבצים שנוצרו:

✅ `admin/index.html` - דף הכניסה לממשק ניהול התוכן
✅ `static/admin/config.yml` - קובץ תצורה של Netlify CMS
✅ `netlify.toml` - הגדרות Netlify
✅ `.gitignore` - קבצים להתעלמות ב-Git
✅ `_posts/` - תיקייה למאמרים (תיווצר אוטומטית)
✅ `_media/` - תיקייה למדיה וסרטונים (תיווצר אוטומטית)

---

## 🎯 שלב 1: העלאה ל-GitHub

### א. יצירת Repository ב-GitHub:

1. היכנס ל-https://github.com והיכנס לחשבון שלך
2. לחץ על הכפתור הירוק **"New"** או **"+"** בפינה הימנית העליונה
3. שם Repository: `matzpen-kalkali-website` (או שם אחר שאתה בוחר)
4. בחר **Public** (חינמי) או **Private**
5. **אל תסמן** "Add README file" או "Add .gitignore"
6. לחץ **"Create repository"**

### ב. העלאת הקבצים דרך GitHub Desktop (הכי קל):

1. הורד והתקן [GitHub Desktop](https://desktop.github.com/)
2. התחבר עם חשבון GitHub שלך
3. לחץ **File** > **Add Local Repository**
4. בחר את התיקייה `תיקונים` (איפה שנמצא index.html)
5. לחץ **Publish repository** > בחר את ה-repository שיצרת
6. לחץ **Publish**

### ג. העלאה דרך שורת הפקודה:

פתח PowerShell/Terminal בתיקיית הפרויקט והרץ:

```powershell
# אתחול git
git init

# הוסף את כל הקבצים
git add .

# צור commit ראשון
git commit -m "Initial commit - מצפן כלכלי website"

# חבר ל-GitHub (החלף את your-username בשם המשתמש שלך)
git remote add origin https://github.com/your-username/matzpen-kalkali-website.git

# העלה את הקבצים
git branch -M main
git push -u origin main
```

---

## 🌐 שלב 2: חיבור ל-Netlify

1. היכנס ל-https://app.netlify.com
2. לחץ על **"Sign up"** אם אין לך חשבון (או **"Log in"**)
3. בחר **"Sign up with GitHub"** (הכי קל)
4. אשר הרשאות ל-Netlify לגשת ל-GitHub שלך
5. ב-Dashboard, לחץ על **"Add new site"** > **"Import an existing project"**
6. בחר **GitHub**
7. בחר את ה-repository: `matzpen-kalkali-website`
8. בהגדרות Build:
   - **Branch to deploy**: `main`
   - **Build command**: השאר ריק ❌ (אין build process)
   - **Publish directory**: `.` (נקודה) ✅
9. לחץ **"Deploy site"** 🚀

**האתר יעלה תוך 1-2 דקות!** ✨

---

## 🔐 שלב 3: הפעלת Netlify Identity

1. ב-Netlify Dashboard, לך ל: **Site settings** (⚙️) > **Identity**
2. לחץ על הכפתור **"Enable Identity"**
3. בחלק **"Registration preferences"**:
   - בחר **"Invite only"** (רק אנשים שתזמין יוכלו להיכנס)
4. גלול למטה למקטע **"Services"** ולחץ **"Enable Git Gateway"**
   - אשר את ההרשאות

---

## 👤 שלב 4: הזמנת עצמך כמש administrator

1. ב-Netlify Dashboard, לך ל: **Identity** > **Invite users**
2. הזמן את עצמך:
   - הזן את כתובת האימייל שלך
   - לחץ **"Send invite"**
3. תקבל אימייל מהנושא "You've been invited"
4. לחץ על הקישור באימייל וצור סיסמה
5. **חשוב!** לאחר הכניסה הראשונה, חזור ל-**Identity** > **Users**
6. לחץ על האימייל שלך > **Edit user**
7. הוסף **Role**: `admin` (זה יאפשר לך לערוך את כל התוכן)

---

## ⚙️ שלב 5: עדכון כתובת האתר ב-config.yml

1. ב-Netlify Dashboard, תחת שם האתר שלך, העתק את כתובת האתר (לדוגמה: `matzpen-kalkali-123abc.netlify.app`)
2. פתח את הקובץ `static/admin/config.yml` בעורך הטקסט שלך
3. מצא את השורות:
   ```yaml
   identity_url: https://your-site-name.netlify.app/.netlify/identity
   gateway_url: https://your-site-name.netlify.app/.netlify/git/gateway
   ```
4. החלף `your-site-name.netlify.app` בכתובת האתר שלך (ב-2 המקומות)
5. שמור את הקובץ
6. אם אתה משתמש ב-GitHub Desktop: Commit והעלה (Sync)
7. אם אתה משתמש בשורת פקודה:
   ```powershell
   git add static/admin/config.yml
   git commit -m "Update Netlify CMS config"
   git push
   ```

---

## 📝 שלב 6: כניסה לממשק ניהול התוכן

1. גש לכתובת: `https://your-site-name.netlify.app/admin`
2. התחבר עם האימייל והסיסמה שיצרת
3. **עכשיו תוכל להוסיף ולערוך מאמרים!** 🎉

---

## ✍️ איך להוסיף מאמר חדש:

1. היכנס ל-`/admin` (למשל: `https://your-site.netlify.app/admin`)
2. לחץ על **"מאמרים"** בתפריט השמאלי
3. לחץ על **"New Blog"** (כפתור כחול למעלה)
4. מלא את השדות:
   - **כותרת**: שם המאמר (לדוגמה: "איך לחסוך 100,000 ש״ח")
   - **תאריך פרסום**: בחר תאריך
   - **תיאור קצר**: תיאור שמופיע בכרטיס המאמר
   - **תמונת נושא**: לחץ "Choose an image" והעלה תמונה
   - **זמן קריאה**: כמה דקות (לדוגמה: 5)
   - **ID ייחודי**: אותיות קטנות באנגלית, ללא רווחים (לדוגמה: `how-to-save-100k`)
   - **תוכן המאמר**: כתוב את המאמר ב-Markdown (יש עורך עם תפריט עזרה)
5. לחץ **"Save"** (כפתור כחול למעלה)
6. המאמר יופיע באתר תוך 1-2 דקות! ✨

---

## 🎬 איך להוסיף סרטון/מדיה:

1. היכנס ל-`/admin`
2. לחץ על **"מדיה וסרטונים"**
3. לחץ **"New Media"**
4. מלא:
   - **כותרת**: שם הסרטון
   - **סוג מדיה**: בחר (סרטון, פודקאסט, וכו')
   - **קישור**: העתק קישור יוטיוב או פלטפורמה אחרת
   - **תיאור**: הסבר קצר
   - **תמונה**: תמונה מקדימה
5. לחץ **"Save"**

---

## 💡 טיפים:

- **Markdown**: המאמרים נכתבים ב-Markdown. [מדריך קצר](https://www.markdownguide.org/cheat-sheet/)
- **תמונות**: תמונות שתעלה יישמרו אוטומטית בתיקיית `images/`
- **עדכונים**: כל שינוי שתשמור יתעדכן אוטומטית באתר תוך דקה-שתיים
- **גיבוי**: כל המאמרים נשמרים ב-GitHub, אז יש לך גיבוי אוטומטי!

---

## ❓ בעיות נפוצות:

**"לא יכול להיכנס ל-/admin"**
- ודא שהפעלת Identity ו-Git Gateway
- ודא שהזמנת את עצמך וצירת סיסמה
- ודא שעדכנת את כתובת האתר ב-config.yml

**"המאמר לא מופיע באתר"**
- המאמרים נשמרים ב-`_posts/` אבל צריך לעדכן את `index.html` כדי לטעון אותם
- בשלב זה, המאמרים נשמרים ב-GitHub ואפשר לערוך אותם דרך Netlify CMS
- לעדכון ה-index.html לטעינת מאמרים מהקבצים - זה דורש קצת עבודת תכנות

**"אין לי הרשאות"**
- ודא שהוספת Role `admin` לחשבון שלך ב-Identity > Users

---

## 📞 תמיכה:

- [תיעוד Netlify CMS](https://www.netlifycms.org/docs/)
- [תיעוד Netlify](https://docs.netlify.com/)
- [Netlify Community Forum](https://answers.netlify.com/)

---

**בהצלחה! 🎉**

אם יש שאלות, אני כאן לעזור!

